{
  "api_provider": {
    "description": "视频分析 API",
    "hint": "配置用于视频理解的 Gemini API 接入方式。可添加官方接口或 OpenAI 兼容中转站。",
    "type": "template_list",
    "templates": {
      "official": {
        "name": "Gemini 官方接口",
        "hint": "使用 Google AI Studio API Key，通过 x-goog-api-key 鉴权直连官方接口。",
        "display_item": "name",
        "hide_hint_in_list": true,
        "items": {
          "name": {
            "description": "供应商名称",
            "hint": "用于标识此接入方，格式：供应商名称/模型名称。",
            "type": "string",
            "default": "gemini-official"
          },
          "api_key": {
            "description": "API Key",
            "hint": "填写 Google AI Studio 的 API Key。",
            "type": "string",
            "default": ""
          },
          "model": {
            "description": "模型名称",
            "hint": "推荐 gemini-2.0-flash 或 gemini-2.5-flash。",
            "type": "string",
            "default": "gemini-2.0-flash"
          },
          "timeout": {
            "description": "超时时间（秒）",
            "hint": "Gemini 处理视频通常需要 60-120 秒，建议不低于 180。",
            "type": "int",
            "slider": { "min": 60, "max": 600, "step": 30 },
            "default": 180
          }
        }
      },
      "openai_compat": {
        "name": "OpenAI 兼容中转站",
        "hint": "支持 Gemini generateContent 或 OpenAI chat/completions 接口的中转站，通过 Bearer 鉴权。模型名含 gemini 时自动走 Gemini 协议，其他模型（如 qwen-vl）自动走 OpenAI 兼容协议。",
        "display_item": "name",
        "hide_hint_in_list": true,
        "items": {
          "name": {
            "description": "供应商名称",
            "hint": "用于标识此接入方，格式：供应商名称/模型名称。",
            "type": "string",
            "default": ""
          },
          "api_key": {
            "description": "API Key / Token",
            "hint": "填写中转站对应的访问密钥。",
            "type": "string",
            "default": ""
          },
          "base_url": {
            "description": "Base URL",
            "hint": "填写中转站地址，如 https://xxx.com/v1。",
            "type": "string",
            "default": ""
          },
          "model": {
            "description": "模型名称",
            "hint": "模型名含 gemini 走 Gemini 协议；其他模型（qwen-vl、gpt-4o 等）走 OpenAI 兼容协议。",
            "type": "string",
            "default": "gemini-2.0-flash"
          },
          "protocol": {
            "description": "协议（可选）",
            "hint": "留空自动判断：模型名含 gemini 走 Gemini 协议，否则走 OpenAI 兼容协议。可强制指定 gemini 或 openai。",
            "type": "string",
            "default": "",
            "collapsed": true
          },
          "timeout": {
            "description": "超时时间（秒）",
            "hint": "Gemini 处理视频通常需要 60-120 秒，建议不低于 180。",
            "type": "int",
            "slider": { "min": 60, "max": 600, "step": 30 },
            "default": 180
          }
        }
      }
    }
  },
  "analysis": {
    "description": "分析设置",
    "hint": "配置视频分析行为和文件限制。",
    "type": "object",
    "items": {
      "model": {
        "description": "分析模型",
        "hint": "格式为「供应商名称/模型名称」，如 gemini-official/gemini-2.0-flash。留空或格式错误时自动使用第一个接入方。",
        "type": "string",
        "default": ""
      },
      "system_prompt": {
        "description": "分析系统提示词",
        "hint": "发给 Gemini 的分析指令，告诉它如何描述视频。用于自动分析和 LLM 工具触发。",
        "type": "text",
        "default": "像一个朋友随便聊聊这段视频，突出你最想说的。不要列表。60字以内。"
      },
      "separate_prompts": {
        "description": "指令触发使用不同提示词",
        "hint": "开启后，「/分析视频」指令触发时使用下方独立的提示词，自动分析和 LLM 工具仍使用上方提示词。",
        "type": "bool",
        "default": false
      },
      "command_system_prompt": {
        "description": "指令触发提示词",
        "hint": "仅「/分析视频」指令触发时使用。",
        "type": "text",
        "default": "像一个朋友随便聊聊这段视频，突出你最想说的。不要列表。60字以内。",
        "collapsed": true,
        "condition": {
          "separate_prompts": true
        }
      },
      "enable_llm_tool": {
        "description": "启用 LLM 工具调用",
        "hint": "开启后 LLM 可在对话中主动调用视频分析工具，无需用户显式命令。",
        "type": "bool",
        "default": true
      },
      "debug": {
        "description": "调试模式",
        "hint": "开启后，自动分析和缓存操作会在日志中输出详细信息，方便排查问题。",
        "type": "bool",
        "default": false
      },
      "max_file_size_mb": {
        "description": "最大文件大小（MB）",
        "hint": "文件总大小上限，超过直接拒绝。Files API 免费层上限 2GB，建议 ≤ 200MB。",
        "type": "int",
        "slider": { "min": 1, "max": 512, "step": 8 },
        "default": 100
      },
      "max_inline_size_mb": {
        "description": "内嵌传输上限（MB）",
        "hint": "小于等于此大小的视频用内嵌传输（请求体含 base64 膨胀约 33%，官方总请求体上限 20MB）；超过则走 Files API。",
        "type": "int",
        "slider": { "min": 1, "max": 19, "step": 1 },
        "default": 15,
        "collapsed": true
      },
      "use_files_api": {
        "description": "启用 Files API（大视频上传）",
        "hint": "超过内嵌上限的视频通过官方 Files API（resumable 上传）处理，免费层单文件 2GB。此开关仅对 Gemini 官方接口生效；中转站接入时会自动跳过 Files API（无论开关状态），改走自动压缩或报错提示，无需手动关闭。",
        "type": "bool",
        "default": true,
        "collapsed": true
      },
      "max_cached_files": {
        "description": "每会话缓存视频数上限",
        "hint": "每个会话最多缓存多少个视频条目，超出后自动丢弃最旧的，防止长会话内存膨胀。",
        "type": "int",
        "slider": { "min": 5, "max": 200, "step": 5 },
        "default": 50,
        "collapsed": true
      },
      "auto_compress": {
        "description": "自动压缩超限视频",
        "hint": "中转站/OpenAI 兼容协议且视频超过内嵌上限时，用 ffmpeg 压缩后再分析。需 ffmpeg（系统安装或平台日志页安装 imageio-ffmpeg，依赖较大约 80MB）；未检测到 ffmpeg 时自动跳过并提示。",
        "type": "bool",
        "default": false,
        "collapsed": true
      },
      "compress_max_duration": {
        "description": "压缩前截取时长上限（秒）",
        "hint": "超过此时长的视频先无损截取前 N 秒再压缩，防止长视频压不进内嵌上限。",
        "type": "int",
        "slider": { "min": 15, "max": 600, "step": 15 },
        "default": 120,
        "collapsed": true,
        "condition": {
          "auto_compress": true
        }
      },
      "compress_resolution": {
        "description": "压缩分辨率（高度）",
        "hint": "视频高度压缩到该值（如 720/480），宽度等比缩放。",
        "type": "int",
        "slider": { "min": 240, "max": 1080, "step": 120 },
        "default": 720,
        "collapsed": true,
        "condition": {
          "auto_compress": true
        }
      },
      "compress_crf": {
        "description": "压缩质量（CRF）",
        "hint": "H.264 质量参数，18-32 之间，越大画质越低体积越小。",
        "type": "int",
        "slider": { "min": 18, "max": 32, "step": 1 },
        "default": 28,
        "collapsed": true,
        "condition": {
          "auto_compress": true
        }
      },
      "supported_formats": {
        "description": "支持的视频格式",
        "hint": "只有这些后缀名的文件会被处理（不含点号，小写）。",
        "type": "list",
        "default": ["mp4", "mov", "webm", "avi", "mpeg", "mpg", "flv", "wmv", "3gpp"]
      }
    }
  }
}
